# Server port (defaults to 4000)
PORT=4000

# Comma-separated list of allowed CORS origins. Defaults to "*" (all origins).
# For production set this to your frontend URL, e.g.
# CORS_ORIGIN=https://startaforge.netlify.app
CORS_ORIGIN=*